legacy-peer-deps=true
# Hoist all dependencies to root node_modules for compatibility.
# Required so workspace binaries (e.g. commitlint) are accessible
# from the root and CI workflows can run `pnpm commitlint`.
shamefully-hoist=true
