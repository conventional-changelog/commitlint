legacy-peer-deps=true

# Prevent install scripts from running automatically.
# Most supply chain attacks rely on postinstall scripts; this blocks that vector.
# Trusted packages that need scripts are listed in allowed-scripts.json.
ignore-scripts=true
onlyBuiltDependenciesFile=allowed-scripts.json
